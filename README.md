# 📊 ДТЭК График - Автоматический парсер графиков отключений

Автоматическая система для парсинга и распространения графиков отключений электроэнергии ДТЭК для города Днепр.

## 🎯 Возможности

### 📡 Автоматический парсинг
- ⏰ Проверяет канал [@dnepr_svet_voda](https://t.me/s/dnepr_svet_voda) каждые 30 минут
- 🎯 Умное определение дат (поддерживает "сьогодні", "24 січня", "24.01.2026" и др.)
- 🔄 Выбирает самый актуальный пост автоматически
- 💾 Сохраняет график в формате JSON с датой `DD.MM.YYYY`

### 🤖 Telegram бот
- 🌐 Поддержка 3 языков: Українська, Русский, English
- ⚡ Показывает текущий статус света (есть/нет)
- 📊 Отображает график на сегодня с временем отключений
- 🔔 Автоматические уведомления всем пользователям при изменении графика
- 💬 Простой и понятный интерфейс

## 📁 Структура проекта

```
blackout-schedule/
├── .github/
│   └── workflows/
│       └── update.yml          # GitHub Actions для автообновления
├── update.py                    # Парсер графиков из Telegram
├── bot.py                       # Telegram бот (деплоится отдельно)
├── schedule.json                # Актуальный график (обновляется автоматически)
└── README.md
```

## 🚀 Быстрый старт

### 1️⃣ Настройка GitHub Actions

1. **Fork этого репозитория**

2. **Включите Actions:**
   - Settings → Actions → General
   - Выберите "Read and write permissions"
   - Сохраните

3. **Добавьте Secrets (опционально для уведомлений):**
   - Settings → Secrets and variables → Actions → New repository secret
   - `TG_BOT_TOKEN` - токен вашего Telegram бота
   - `TG_CHAT_ID` - ID чата для уведомлений

4. **Запустите первый раз вручную:**
   - Actions → Update Schedule → Run workflow

### 2️⃣ Деплой Telegram бота

#### На Railway (рекомендуется):

1. Зарегистрируйтесь на [Railway.app](https://railway.app)
2. Создайте новый проект → Deploy from GitHub
3. Выберите репозиторий
4. Добавьте переменные окружения:
   ```
   TOKEN=ваш_telegram_bot_token
   SCHEDULE_URL=https://raw.githubusercontent.com/ваш_username/blackout-schedule/main/schedule.json
   ```
5. Railway автоматически задеплоит бота

#### Локально:

```bash
# Установите зависимости
pip install python-telegram-bot requests

# Экспортируйте переменные
export TOKEN="ваш_telegram_bot_token"
export SCHEDULE_URL="https://raw.githubusercontent.com/.../schedule.json"

# Запустите бота
python bot.py
```

## ⚙️ Конфигурация

### Переменные окружения для `update.py`:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `TG_CHANNEL` | ID Telegram канала | `dnepr_svet_voda` |
| `SCHEDULE_PATH` | Путь к JSON файлу | `schedule.json` |
| `TIMEZONE` | Часовой пояс | `Europe/Kyiv` |
| `TG_LOOKBACK` | Количество постов для проверки | `200` |
| `TG_BOT_TOKEN` | Токен бота (для уведомлений) | - |
| `TG_CHAT_ID` | Chat ID (для уведомлений) | - |

### Переменные окружения для `bot.py`:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `TOKEN` | Токен Telegram бота | **обязательно** |
| `SCHEDULE_URL` | URL к schedule.json | см. код |
| `CACHE_TTL` | Время кэширования (сек) | `300` |
| `CHECK_INTERVAL` | Интервал проверки обновлений (сек) | `300` |
| `PERSISTENCE_FILE` | Файл состояния бота | `bot_state.pickle` |

## 📊 Формат schedule.json

```json
{
  "date": "24.01.2026",
  "timezone": "Europe/Kyiv",
  "groups": {
    "1.1": ["12:00-20:30"],
    "1.2": ["13:00-20:30"],
    "2.1": ["13:00-22:00"],
    "5.1": ["10:00-17:00", "20:30-24:00"]
  }
}
```

## 🔄 Как изменить частоту обновлений

Откройте `.github/workflows/update.yml` и измените `cron`:

```yaml
schedule:
  # Каждые 15 минут
  - cron: '5,20,35,50 * * * *'
  
  # Каждый час
  - cron: '5 * * * *'
  
  # Каждые 2 часа
  - cron: '5 */2 * * *'
```

## 🔔 Уведомления

### Из update.py → вам лично:

Добавьте Secrets в GitHub:
- `TG_BOT_TOKEN` - токен вашего бота
- `TG_CHAT_ID` - ваш Telegram ID

### Из бота → всем пользователям:

Бот автоматически отслеживает изменения `schedule.json` каждые 5 минут и рассылает уведомления всем, кто хоть раз им пользовался.

## 🛠️ Разработка

### Локальное тестирование парсера:

```bash
# Установите зависимости
pip install requests

# Запустите
python update.py

# С настройками
TG_CHANNEL=dnepr_svet_voda TG_LOOKBACK=100 python update.py
```

### Локальное тестирование бота:

```bash
pip install python-telegram-bot requests

export TOKEN="your_token"
export SCHEDULE_URL="https://raw.githubusercontent.com/.../schedule.json"

python bot.py
```

## 📝 Поддерживаемые форматы дат

Парсер понимает следующие форматы в постах:

- ✅ `сьогодні` / `сегодня` - автоматически сегодняшняя дата
- ✅ `24.01.2026` - полная дата
- ✅ `24 січня 2026` - текстовая дата с годом
- ✅ `на 24 січня` - дата без года (год определяется автоматически)
- ✅ `24.01` - короткая дата (год текущий)

## 🐛 Отладка

### Просмотр логов GitHub Actions:

1. Actions → Update Schedule
2. Кликните на запуск
3. Откройте "Run update script"

### Скачивание debug артефактов:

После каждого запуска создаются артефакты с:
- `schedule.json` - результат парсинга
- `debug_page.html` - сохранённая HTML страница канала
- `error_page.html` - HTML при ошибках

Скачать можно внизу страницы запуска workflow.

## 🤝 Вклад в проект

Приветствуются Pull Requests! Основные направления:

- 🐛 Исправления багов
- ✨ Новые возможности
- 📝 Улучшение документации
- 🌐 Новые языки интерфейса

## 📄 Лицензия

MIT License - используйте свободно!

## 🔗 Ссылки

- Канал ДТЭК: [@dnepr_svet_voda](https://t.me/s/dnepr_svet_voda)
- GitHub Actions: [Документация](https://docs.github.com/en/actions)
- python-telegram-bot: [Документация](https://docs.python-telegram-bot.org/)

## 💬 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте раздел [Отладка](#-отладка)
2. Посмотрите логи в Actions
3. Создайте Issue с описанием проблемы

---

**Сделано с ❤️ для жителей Днепра**
